<% @attendances.each do |attendance| %>
      <tr>
        <td><%= attendance.date_time.strftime("%m/%d/%Y %I:%M") %></td>
        <td><%= attendance.employee.fullname %></td>
        <td><%= attendance.type_check %></td>
      </tr>
<% end %>